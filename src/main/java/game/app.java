package game;

import java.awt.Window;

import entity.player;

// import entity.player;

public class app {
    public static void main( String[] args ){
        window newGame = new window();
        // mainScreen newmMainScreen  = new mainScreen(newGame.curentGame);
        // System.out.println(newmMainScreen.levelCount);
        // newGame.curentGame.run();
        // player player = new player(25, 25);
        // nGame.addPlayer(player);
        // while (true){

        // }
    }
}
